import RemoveImagesPipe from './remove-images.pipe';

describe('RemoveImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
