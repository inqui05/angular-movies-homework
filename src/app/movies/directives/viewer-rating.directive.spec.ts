import ViewerRatingDirective from './viewer-rating.directive';

describe('ViewerRatingDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewerRatingDirective();
    expect(directive).toBeTruthy();
  });
});
